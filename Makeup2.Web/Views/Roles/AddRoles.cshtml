@model Makeup2.Web.Models.ApplicationUser
@{
    ViewBag.Title = "AddRoles";
}
<p class="mt-3 pt-lg-5">
<p class="mt-3 pt-lg-5"></p>
    <div class="container">
        <div class="row justify-content">
            <div class="col-md-6">
                <div class="card py-0">
                    <div class="card-header bg-principal">
                        <h4 class="m-1 text-white">Agrega un rol</h4>
                    </div>
                    <div class="card-body">
                        <form class="" action="@Url.Action("AddRoles", "Roles")" method="post">
                            <div class="form-group">
                                <label class="col-md-10">Usuario</label>
                                <div class="col-md-10">
                                    <input type="text" value="@Model.UserName" />
                                </div>
                            </div>

                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
